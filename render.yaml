services:
  - type: web
    name: news-summary-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: uvicorn summarize_conclusion.csv:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENAI_API_KEY
        sync: false